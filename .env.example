# Marketing / SEM tags — all OPTIONAL and inert until set (see components/MarketingTags.tsx).
# Set these in Vercel → Project → Settings → Environment Variables (Production).
# EU/GDPR: before enabling, add a cookie-consent banner and update /cookies.

# Google Analytics 4 measurement id
NEXT_PUBLIC_GA_ID=

# Google Ads tag id (for conversion tracking / remarketing)
NEXT_PUBLIC_GOOGLE_ADS_ID=

# Meta (Facebook/Instagram) Pixel id
NEXT_PUBLIC_META_PIXEL_ID=
